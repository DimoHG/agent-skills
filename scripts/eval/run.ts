#!/usr/bin/env tsx
/**
 * Paired A/B evaluation runner for agent skills.
 *
 * This file is a scaffolding stub. The full implementation lands in a
 * follow-up commit to keep the runner reviewable in isolation from the
 * project wiring (tsconfig, types, package.json scripts, .gitignore).
 */

console.error(
  "scripts/eval/run.ts: runner not yet implemented (scaffold only).",
);
process.exit(2);
